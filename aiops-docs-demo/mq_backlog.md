# 消息队列消费堆积处置

## 现象

消费堆积持续增长，告警 `MQBacklogGrowing` firing，延迟越拖越大。

## 排查

1. 看堆积：队列 lag 指标（如 `kafka_consumergroup_lag`）确认堆积速度和总量。
2. 看消费端：消费者 CPU/GC 是否正常，`top` + 日志看是否下游变慢拖住消费。
3. 看生产端：上游流量是否突增（对比昨日同时段），区分消费变慢还是生产暴涨。

## 处置

- 短期：横向扩容消费者，加分区并发；无效则限流生产端。
- 长期：消费加超时+熔断，lag 超阈值自动扩容，死信队列收容毒消息。
